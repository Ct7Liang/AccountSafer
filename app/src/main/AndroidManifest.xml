<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ct7liang.accountsafer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.EntryActivity" />
        <activity android:name=".activity.CreateEntryPwActivity" />
        <activity android:name=".activity.CreateQueryPwActivity" />
        <activity android:name=".activity.EditEntryPwActivity" />
        <activity android:name=".activity.EditQueryPwActivity" />
        <activity android:name=".activity.AddAccountActivity" />
        <activity android:name=".activity.AccountDetailActivity" />
        <activity android:name=".activity.EditAccountActivity" />
        <activity android:name=".activity.DataBackUpActivity" />
        <activity android:name=".activity.AboutAppActivity"></activity>
    </application>

</manifest>